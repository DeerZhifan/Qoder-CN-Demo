---
trigger: manual
alwaysApply: false
---

# 变更说明生成器

当我用 @rule 引用本规则时，请根据「本次会话改动的文件 / git diff」，按下面模板生成一份《变更说明》。
要求：中文输出；只写真实发生的改动，缺项写"无"，不要编造；接口和表结构要与代码严格对齐。

## 《变更说明》模板

### 一、基本信息
- 变更标题：
- 关联需求/任务：
- 变更类型：☐新增功能 ☐缺陷修复 ☐重构 ☐配置变更（可多选）
- 影响范围：☐后端(com.kb.manager.*) ☐前端(src/*) ☐数据库

### 二、变更内容（按层分组，要点式）
- 后端：
- 前端：
- 数据库：

### 三、接口变更（新增/修改/废弃都要列）
| 变更 | 方法 | 路径 | 说明 | 主要入参 | 返回 Result<T> 的 T | 是否破坏性 |
|------|------|------|------|----------|--------------------|-----------|
| 新增 | GET | /api/documents/... |  |  |  | 否 |
> 提醒：所有接口统一返回 Result<T>；破坏性变更（改路径/改字段/删接口）必须标注并说明兼容方案。

### 四、数据库变更
- schema.sql 改动：建表/改表/加字段/索引（新增字段注明类型、是否可空、默认值）
- 逻辑删除字段 deleted、状态字段 status 等是否受影响
- 是否需要数据迁移；无则写"无"

### 五、配置/依赖变更
- application.yml / application-dev.yml：
- 依赖：pom.xml（后端）、package.json（前端）：
- 无则写"无"

### 六、回滚方案
- 代码回滚：回退到提交/分支 ____
- 数据库回滚：对应的回滚 SQL 或处理方式（已发布文档、版本记录需特别说明）
- 风险与注意点：

### 七、验证清单
- [ ] 后端单测通过（mvn test，含 KbDocumentServiceTest 等）
- [ ] 关键接口手测（附 curl 或操作步骤）
- [ ] 文档状态机边界已验（DRAFT→PUBLISHED→OFFLINE、软删除可恢复）
- [ ] 前端本地可跑（npm run dev）核心流程无报错